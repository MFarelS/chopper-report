{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "source": "packages/firebase-functions/webpack",
    "predeploy": ["cd packages/firebase-functions && npm run build"]
  }
}
